# Search Bot — Telegram бот для поиска ремней
 
## 📦 Установка
 
### 1. Клонирование репозитория
 
```powershell
git clone "https://github.com/Kirill-Eltsov/Search-bot.git"
```
 
### 2. Создание виртуального окружения
 
```powershell
# Создание виртуального окружения
py -3 -m venv .venv
 
# Активация виртуального окружения
.\.venv\Scripts\Activate.ps1
 
```
 
### 3. Установка зависимостей
 
```powershell
 
# Установка зависимостей
pip install -r requirements.txt
```
 
## 🗄️ Настройка базы данных
 
1. **Создание базы данных:**
   ```powershell
   # Создать БД
   psql -U postgres -h localhost -c "CREATE DATABASE beltimpex WITH ENCODING 'UTF8';"
   ```
 
2. **Применение схемы и тестовых данных:**
   ```powershell
   # Применить схему
   psql -U postgres -h localhost -d beltimpex -f db\schema.sql
 
   # Загрузить тестовые данные
   psql -U postgres -h localhost -d beltimpex -f db\seed.sql
   ```
 
### Настройка подключения к БД
 
По умолчанию бот использует следующие параметры:
- **Хост:** `localhost`
- **Порт:** `5432`
- **База данных:** `beltimpex`
- **Пользователь:** `postgres`
- **Пароль:** `postgres`
 
Если вы используете другие параметры, задайте переменную окружения:
 
```powershell
$env:PGHOST = "localhost"
$env:PGPORT = "5432"
$env:PGDATABASE = "beltimpex"
$env:PGUSER = "postgres"
$env:PGPASSWORD = "ваш_пароль"
 
```
 
## ⚙️ Настройка конфигурации
 
### 1. Создание файла `config.py`
 
```python
# Получите токен у @BotFather в Telegram
BOT_TOKEN = "YOUR_BOT_TOKEN_HERE"
 
# Контакты менеджера (замените на реальные контакты)
MANAGER_CONTACTS = """
 
Telegram: @manager_username
 
"""
 
# OpenAI API ключ для извлечения параметров из запросов пользователей
OPENAI_API_KEY = "YOUR_OPENAI_API_KEY_HERE"
```
 
## 🚀 Запуск бота
 
### Запуск бота
 
```powershell
python bot.py
```
 
Если всё настроено правильно, вы увидите:
```
Бот запущен...
```