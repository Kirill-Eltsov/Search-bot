# Search Bot — Telegram бот для поиска ремней

Telegram-бот для поиска синхронных и клиновых ремней в базе данных с поддержкой естественного языка через OpenAI.

## 📋 Содержание

- [Требования](#требования)
- [Установка](#установка)
- [Настройка базы данных](#настройка-базы-данных)
- [Настройка конфигурации](#настройка-конфигурации)
- [Запуск бота](#запуск-бота)
- [Использование](#использование)

## 🔧 Требования

- Python 3.8+
- PostgreSQL 15+ (или Docker)
- Telegram Bot Token (от @BotFather)
- OpenAI API Key (опционально, для работы с естественным языком)

## 📦 Установка

### 1. Клонирование репозитория

```powershell
cd "C:\Users\Кирилл\Desktop\project\Search-bot"
```

### 2. Создание виртуального окружения

```powershell
# Создание виртуального окружения
py -3 -m venv .venv

# Активация виртуального окружения
.\.venv\Scripts\Activate.ps1

# Если активация блокируется политикой, выполните:
# Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
```

### 3. Установка зависимостей

```powershell
# Обновление pip
python -m pip install -U pip

# Установка зависимостей
pip install -r requirements.txt
```

## 🗄️ Настройка базы данных

### Вариант 1: Установка PostgreSQL (рекомендуется)

1. **Скачайте PostgreSQL:**
   - Официальный установщик: [Download PostgreSQL (EDB)](https://www.enterprisedb.com/downloads/postgres-postgresql-downloads)
   - Выберите версию 15+ для Windows x86-64

2. **Установка:**
   - Запустите установщик
   - Оставьте порт по умолчанию: `5432`
   - При создании пароля суперпользователя `postgres` установите: `postgres` (или другой, если хотите изменить настройки подключения)
   - Установите pgAdmin 4 (включается в установщик)

3. **Создание базы данных:**
   ```powershell
   # Создать БД
   psql -U postgres -h localhost -c "CREATE DATABASE beltimpex WITH ENCODING 'UTF8';"
   ```

4. **Применение схемы и тестовых данных:**
   ```powershell
   # Применить схему
   psql -U postgres -h localhost -d beltimpex -f db\schema.sql

   # Загрузить тестовые данные
   psql -U postgres -h localhost -d beltimpex -f db\seed.sql
   ```

### Вариант 2: Docker (быстрый старт)

```powershell
# Запустить PostgreSQL контейнер
docker run --name pg-beltimpex -e POSTGRES_PASSWORD=postgres -e POSTGRES_DB=beltimpex -p 5432:5432 -d postgres:15

# Подождать 10-20 секунд, затем применить схему
docker exec -i pg-beltimpex psql -U postgres -d beltimpex < db\schema.sql

# Загрузить тестовые данные
docker exec -i pg-beltimpex psql -U postgres -d beltimpex < db\seed.sql
```

### Настройка подключения к БД

По умолчанию бот использует следующие параметры:
- **Хост:** `localhost`
- **Порт:** `5432`
- **База данных:** `beltimpex`
- **Пользователь:** `postgres`
- **Пароль:** `postgres`

Если вы используете другие параметры, задайте переменную окружения:

```powershell
# Вариант 1: Через отдельные переменные
$env:PGHOST = "localhost"
$env:PGPORT = "5432"
$env:PGDATABASE = "beltimpex"
$env:PGUSER = "postgres"
$env:PGPASSWORD = "ваш_пароль"

# Вариант 2: Через DATABASE_URL
$env:DATABASE_URL = "postgresql://postgres:ваш_пароль@localhost:5432/beltimpex"
```

## ⚙️ Настройка конфигурации

### 1. Создание файла `config.py`

Скопируйте пример конфигурации:

```powershell
# Windows PowerShell
Copy-Item config.py.example config.py
```

Или создайте файл `config.py` вручную:

```python
# Токен Telegram бота
# Получите токен у @BotFather в Telegram
BOT_TOKEN = "YOUR_BOT_TOKEN_HERE"

# Контакты менеджера (замените на реальные контакты)
MANAGER_CONTACTS = """
📞 Контакты менеджера:

Telegram: @manager_username

"""

# OpenAI API ключ для извлечения параметров из запросов пользователей
OPENAI_API_KEY = "YOUR_OPENAI_API_KEY_HERE"
```

### 2. Получение токена Telegram бота

1. Откройте Telegram и найдите бота [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot` или `/token`
3. Следуйте инструкциям для создания бота или получения токена
4. Скопируйте полученный токен и вставьте в `config.py` вместо `YOUR_BOT_TOKEN_HERE`

### 3. Получение OpenAI API ключа (опционально)

1. Зарегистрируйтесь на [OpenAI Platform](https://platform.openai.com/)
2. Перейдите в раздел [API Keys](https://platform.openai.com/api-keys)
3. Создайте новый ключ
4. Скопируйте ключ и вставьте в `config.py` вместо `YOUR_OPENAI_API_KEY_HERE`

> **Примечание:** OpenAI API используется только для запросов в свободной форме. Если ключ не указан, бот будет работать только с формализованными запросами.

## 🚀 Запуск бота

### 1. Активация виртуального окружения

```powershell
cd "C:\Users\Кирилл\Desktop\project\Search-bot"
.\.venv\Scripts\Activate.ps1
```

### 2. Проверка конфигурации

```powershell
# Проверить, что токен задан
python -c "from config import BOT_TOKEN; print('Токен задан:', bool(BOT_TOKEN) and BOT_TOKEN != 'YOUR_BOT_TOKEN_HERE')"
```

### 3. Запуск бота

```powershell
python bot.py
```

Если всё настроено правильно, вы увидите:
```
Бот запущен...
```

## 📱 Использование

### Основные команды

- `/start` — Начать работу с ботом (верификация)
- `/operator` — Получить контакты оператора

### Поиск товаров

#### Формализованные запросы (без ИИ):

**Синхронные ремни:**
- `8008M` — ремень длиной 800 мм, профиль 8M
- `177814M=55` — ремень длиной 1778 мм, профиль 14M, ширина 55 мм
- `240L` — ремень длиной 240 мм, профиль L
- `1700H` — ремень длиной 1700 мм, профиль H

**Клиновые ремни:**
- `SPA2000` — ремень профиль SPA, длина 2000 мм
- `B85` — ремень профиль B, длина 85 дюймов
- `3V2000` — ремень профиль 3V, длина 2000 мм

#### Свободные запросы (с ИИ):

- "нужен синхронный ремень длиной 800 миллиметров профиль 8M"
- "ищу клиновой ремень SPA длиной 2000 миллиметров"
- "нужен ремень B85"

## 🧪 Тестирование с mock-режимом (без трат на OpenAI)

Для тестирования без реальных запросов к OpenAI используйте mock-режим:

```powershell
# Установить mock-данные
$env:OPENAI_MOCK_JSON = '{"kind":"synchronous","length_mm":800,"profile":"8M","width_mm":null}'

# Запустить бота
python bot.py
```

Примеры mock-данных:

**Синхронный ремень:**
```powershell
$env:OPENAI_MOCK_JSON = '{"kind":"synchronous","length_mm":800,"profile":"8M","width_mm":null}'
```

**Клиновой ремень:**
```powershell
$env:OPENAI_MOCK_JSON = '{"kind":"vbelt","length_mm":2000,"profile":"SPA","width_mm":null}'
```

**Синхронный с шириной:**
```powershell
$env:OPENAI_MOCK_JSON = '{"kind":"synchronous","length_mm":1778,"profile":"14M","width_mm":55}'
```

Чтобы отключить mock-режим:
```powershell
Remove-Item Env:OPENAI_MOCK_JSON
```

## 📂 Структура проекта

```
Search-bot/
├── bot.py                 # Главный файл бота
├── config.py             # Конфигурация (создать из config.py.example)
├── config.py.example     # Пример конфигурации
├── requirements.txt      # Зависимости Python
├── db_connection.py     # Подключение к БД
├── search_service.py    # Логика поиска товаров
├── ai_service.py        # Интеграция с OpenAI
├── handlers/            # Обработчики сообщений
│   ├── auth.py         # Авторизация
│   ├── menu.py         # Главное меню
│   ├── operator.py     # Контакты оператора
│   └── text.py         # Обработка текстовых запросов
└── db/                  # SQL скрипты
    ├── schema.sql       # Схема БД
    └── seed.sql         # Тестовые данные
```

## 🔍 Алгоритм поиска

1. **Парсер** — для формализованных запросов (8008M, SPA2000, B85)
2. **Regex** — для извлечения токенов из текста ("ищу ремень 8008M")
3. **OpenAI ИИ** — для свободных запросов ("нужен синхронный ремень длиной 800 миллиметров")

## ⚠️ Устранение неполадок

### Бот не запускается

- Проверьте, что PostgreSQL запущен
- Проверьте, что токен бота указан в `config.py`
- Убедитесь, что виртуальное окружение активировано

### Ошибка подключения к БД

```powershell
# Проверьте параметры подключения
python -c "from db_connection import get_connection; conn = get_connection(); print('Подключено!')"
```

### Ошибка "InvalidToken"

- Проверьте токен в `config.py`
- Убедитесь, что токен не содержит лишних пробелов
- Проверьте токен в браузере: `https://api.telegram.org/bot<ТОКЕН>/getMe`

### OpenAI не работает

- Проверьте баланс на [OpenAI Platform](https://platform.openai.com/account/usage)
- Используйте mock-режим для тестирования без трат
- Проверьте доступность API: `curl https://api.openai.com/v1/models`

## 📝 Лицензия

Проект для внутреннего использования.

## 📞 Поддержка

Для вопросов и поддержки обращайтесь к оператору через команду `/operator` в боте.

